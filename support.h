#ifndef SUPPORT_H
#define SUPPORT_H

#include <iostream>
#include <string>
#include <cmath>

using std::cout;
using std::endl;
using std::string;
using std::pow;
using std::stoi;

float bin2float(const string& binary);

#endif